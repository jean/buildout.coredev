[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "robotframework-browser"
version = "20.0.0"
description = "Stub for Termux/Android - robotframework-browser requires a browser and cannot run on Android"
